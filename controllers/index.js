const accounts = require('./accounts');
const bugs = require('./bugs');
const calendars = require('./calendars');
const clients = require('./clients');
const diaries = require('./diaries');
const files = require('./files');
const finances = require('./finances');
const frameworks = require('./frameworks');
const functions = require('./functions');
const groups = require('./groups');
const jobs = require('./jobs');
const music = require('./music');
const projects = require('./projects');
const reports = require('./reports');
const skills = require('./skills');
const tools = require('./tools');
const uploads = require('./uploads');
const users = require('./users');
const videos = require('./videos');

module.exports = {
  accounts, bugs, calendars, clients, diaries, files, finances, frameworks, functions, groups, jobs, music, projects, reports, skills, tools, uploads, users, videos
};